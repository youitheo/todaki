# todaqi.com — "토닥이" 키워드 엔티티 & SEO 점검 리포트

> 점검일: 2026-06-29
> 대상: https://todaqi.com (토닥이 / 여성전용마사지 출장 케어 서비스)
> 점검 키워드: **토닥이**

---

## 0. 점검 방식과 제약 (먼저 읽어주세요)

본 실행 환경의 네트워크 정책이 `todaqi.com` 호스트 직접 접근을 차단(403 policy
denial)하여 **라이브 크롤링은 수행하지 못했습니다.** 따라서 본 리포트는
**검색엔진(Google)에 색인된 SERP 데이터**(title 태그·URL 구조·노출 스니펫)를
근거로 작성했습니다.

- ✅ 확인 가능: 검색엔진이 인식하는 페이지 제목, 사이트 구조, URL 패턴, 색인 상태,
  키워드/엔티티 경쟁 환경
- ⚠️ 미확인(별도 크롤링 필요): `meta description` 원본, `og:*` / Twitter Card,
  `canonical`, `robots`/`X-Robots-Tag`, `JSON-LD`(Organization/LocalBusiness/FAQPage 등),
  `hreflang`, H1~H3 실제 마크업, 내부링크 구조, Core Web Vitals, sitemap/robots.txt

→ 아래 "추가 검증 체크리스트"를 크롤링 허용 환경에서 실행해 보강 권장.

---

## 1. 엔티티(Entity) 진단 — 가장 중요한 이슈

### 1-1. "토닥이"는 브랜드가 아니라 **카테고리 일반어**다
검색 결과 다수 출처가 "**토닥이 = 여성전용마사지**"를 *동의어*로 설명합니다.
즉 "토닥이"는 todaqi.com 고유의 **브랜드 엔티티가 아니라, 업종(서비스 카테고리)을
가리키는 일반 명사**로 굳어져 있습니다. ("토닥토닥 = 다독이다"라는 일상어에서 파생.)

**영향**: 일반어/카테고리어는 특정 사이트가 "브랜드 엔티티"로 **소유(own)할 수 없습니다.**
Google Knowledge Graph가 todaqi.com을 "토닥이"의 대표 개체로 연결할 근거가 약합니다.

### 1-2. 동일 키워드 경쟁자가 매우 많음 (엔티티 희석)
"토닥이" 키워드를 **동일하게** 쓰는 사이트/앱이 SERP에 다수 존재:

| 유형 | 예시 |
|---|---|
| 경쟁 도메인 | todack22.com(강남토닥이), todacke.com, todacktop.com, todak-2.com(토닥이닷컴) |
| 서브도메인/무료호스팅 유사군 | todaki.dothome.co.kr, todaki.creatorlink.net, sexytodak2.creatorlink.net |
| 앱 | Google Play "토닥이 - 최초 여성전용 마사지" (com.yeodae.toadak) |
| 블로그/정보성 | maddago.com/blog 등 "토닥이 뜻" 콘텐츠 |

→ 단일 키워드 "토닥이"의 **엔티티 신호가 수십 개 도메인에 분산**. 브랜드 권위(authority)
집중이 안 됨.

### 1-3. 이름이 비슷한 **다른 업종 엔티티와 충돌**
- **토닥(Todak) — AI 심리상담** (todak.io.kr): 전혀 다른 정상 서비스인데 "토닥/토닥이"
  표기가 겹쳐 검색 의도(search intent)가 섞임. 사용자가 "토닥이"를 감정 케어/상담으로
  검색할 경우 의도 불일치 발생.

### 1-4. **로마자 표기 불일치**로 신호 분산 (치명적)
같은 단어를 외부에서 제각각 표기 → 브랜드 엔티티 신호가 쪼개짐:
`todaqi`(본 도메인) · `todaki` · `todak` · `toadak`(앱 패키지) · `todack`
→ 외부 인용/백링크의 앵커·URL이 일관되지 않아 **하나의 개체로 통합되지 못함.**

> **엔티티 결론**: todaqi.com은 "토닥이"라는 **일반어 + 과포화 경쟁 + 표기 불일치 +
> 동음 타업종 충돌**이라는 4중 불리 조건에 놓여 있음. 현재 구조로는 검색엔진이
> "토닥이 ≡ todaqi.com"으로 인식할 가능성이 낮음. **고유 브랜드 토큰 확립이 최우선 과제.**

---

## 2. 기술/구조 SEO 진단 (색인 기반)

### 2-1. 잘 되어 있는 점 (강점)
색인된 URL/제목으로 볼 때 **사이트 아키텍처와 title 설계는 양호**합니다.

- **체계적 디렉터리 구조**: `/price/`, `/review/`, `/faq/`, `/introduction/profile/`,
  `/blog/`, `/커뮤니티/`, `/게시판/`, `/고객-후기/`
- **지역 랜딩 페이지(로컬 SEO)**: `/local/pyeongtaek/`, `/local/incheon/` +
  "평택토닥이/인천토닥이/포천토닥이" 프로그래매틱 페이지군
- **서브도메인 전략**: `casa.todaqi.com`(카사노바), `incheon.todaqi.com`
- **title 태그 패턴 우수**: 브랜드 + 키워드 + 가치제안 + 파이프(|) 구분
  - 예) `평택토닥이 공식 예약센터 | No.1 고덕 비전동 여성전용 방문 홈케어`
  - 예) `토닥이 가격 안내 | 100% 정찰제 및 인기 프리미엄 C코스 상세 요금`
- **검색 의도 커버리지**: 가격/후기/FAQ/프로필/지역 등 퍼널 단계별 페이지 보유

### 2-2. 우려/리스크 (약점)
- **URL에 한글 인코딩 다수**: `/%EA%B3%A0%EA%B0%9D-%ED%9B%84%EA%B8%B0/`
  (`/고객-후기/`), `/커뮤니티/`, `/게시판/` → 가독성·공유·일부 도구 호환성 저하.
  영문 슬러그(`/reviews/`, `/community/`) 권장.
- **URL 패턴 혼재**: `/review/` 와 `/고객-후기/` 와 `/todaki-c-course-review-guide/` 와
  `?kboard_content_redirect=20`, `?mod=document&uid=18` 등 **후기 관련 URL이 4종 이상**
  → 중복/유사 콘텐츠 및 **canonical 분산** 위험. 색인 카니발라이제이션 점검 필요.
- **서브도메인 vs 서브디렉터리 혼용**: `incheon.todaqi.com` 과 `/local/incheon/` 이
  동시 존재 → 동일 지역 의도가 두 곳으로 갈림. 권위 분산 + 중복 위험. **하나로 통일 권장**
  (지역 SEO는 일반적으로 서브디렉터리 통합이 권위 누적에 유리).
- **쿼리스트링 기반 게시판 URL**(`?mod=document&uid=`, `?kboard_content_redirect=`):
  KBoard 류 워드프레스 게시판으로 추정 → 파라미터 URL 색인/중복 관리 필요.
- **업종 특성(성인 인접/YMYL 인접)**: Google이 민감 카테고리로 분류 시 노출 억제,
  광고(AdSense) 제한, 안전검색 필터 영향 가능. 백링크 품질이 스팸성일 경우 패널티 위험.

---

## 3. "토닥이" 키워드 SEO — 우선순위 권고안

### 🔴 P0 — 엔티티 확립 (가장 시급)
1. **고유 브랜드 토큰 만들기**: "토닥이" 단독으로는 소유 불가.
   `토닥이 공식` / `토닥이 [고유수식어]` 형태의 **변별력 있는 브랜드명**을 정해
   전 채널에서 100% 일관 사용. (예: 항상 "토닥이 공식 todaqi.com"로 표기)
2. **로마자 표기 단일화**: 모든 외부 인용·SNS·프로필에서 `todaqi` 하나로 통일.
   `todaki/todak/toadak` 혼용 중단.
3. **Organization/LocalBusiness JSON-LD 구축** + `sameAs`로 공식 채널(인스타/카톡채널/앱
   등) 연결 → Google에 "이것이 토닥이 대표 개체"라는 명시 신호 제공.
4. **브랜드 검색 SERP 점유**: 공식 사이트 외 SNS/프로필/리뷰 페이지로 첫 화면 장악
   (유사 도메인·스팸 도메인에 브랜드 트래픽 빼앗기지 않도록).

### 🟠 P1 — 기술 SEO 정리
5. **후기 URL 통합 + canonical 정리**: 4종 이상으로 흩어진 후기 URL을 대표 URL로
   canonical 지정, 중복 색인 제거.
6. **서브도메인/서브디렉터리 택일**: 지역 페이지를 `/local/{지역}/` 한 체계로 통합하고
   서브도메인은 301 리다이렉트.
7. **URL 영문 슬러그화**: 한글 인코딩 URL → 영문 슬러그로 점진 전환(+301).
8. **sitemap.xml / robots.txt / X-Robots 점검**: 파라미터 게시판 URL 색인 정책 명확화.

### 🟡 P2 — 콘텐츠/구조화 데이터
9. **FAQPage 스키마**(`/faq/`), **Review/AggregateRating 스키마**(후기 페이지),
   **BreadcrumbList** 적용 → 리치 결과 노출 기회.
10. **지역 페이지 본문 차별화**: 평택/인천/포천 등 프로그래매틱 페이지가 템플릿
    복붙이면 thin/duplicate content 위험. 지역별 고유 정보(가능 동네·출장비·후기) 명시.
11. **"토닥이 뜻" 등 정보성 키워드**를 자사 블로그로 선점(현재 외부 블로그가 차지) →
    카테고리 정의 페이지로 엔티티 권위 강화.

---

## 4. 추가 검증 체크리스트 (크롤링 허용 환경에서)
실제 on-page 값 확인이 필요한 항목 — 본 환경에선 정책상 미확인:

- [ ] `<title>` / `meta description` 원본 길이·중복 여부 (전 페이지)
- [ ] `og:title/description/image/type/url`, `twitter:card` 존재·정합성
- [ ] `<link rel="canonical">` 자기참조/교차참조 정확성
- [ ] `meta robots` / `X-Robots-Tag` (noindex 오설정 여부)
- [ ] JSON-LD: Organization / LocalBusiness / FAQPage / Review 유무·유효성
- [ ] `hreflang` (불필요시 제거)
- [ ] H1 단일성·키워드 정합, H2~H3 위계
- [ ] robots.txt / sitemap.xml 접근성·정확성
- [ ] Core Web Vitals (LCP/CLS/INP), 모바일 친화성
- [ ] 백링크 프로필 품질(스팸/유사도메인 링크 여부)
- [ ] `site:todaqi.com` 색인 페이지 수 vs 의도한 페이지 수 (색인 누락/과다)

---

## 5. 한 줄 요약
> **기술/구조 SEO(아키텍처·title·로컬 페이지)는 평균 이상으로 잘 짜여 있으나,
> "토닥이"가 브랜드가 아닌 일반 카테고리어인 데다 동일 키워드 경쟁·로마자 표기
> 불일치·동음 타업종(AI 상담 '토닥') 충돌로 엔티티 신호가 심각하게 희석되어 있다.
> 최우선 과제는 변별력 있는 브랜드 토큰 확립 + Organization/sameAs 구조화 데이터로
> "토닥이 ≡ todaqi.com" 개체를 검색엔진에 명시하는 것이다.**

---
*본 리포트는 egress 정책으로 라이브 크롤링이 불가하여 Google 색인 SERP 데이터 기반으로
작성됨. on-page 원본 태그 검증은 §4 체크리스트 참조.*
